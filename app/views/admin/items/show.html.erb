<h3>商品詳細</h3>
<div>
  <%= attachment_image_tag @item, :image,style: "width:300px; height:200px;" %>
</div>

<div>
  <dl>
    <dt>商品名</dt><dd><%= @item.name %></dd>
    <dt>商品説明</dt><dd><%= @item.introduction %></dd>
    <dt>ジャンル</dt>
      <dd>
        <% genre=Genre.find(@item.genre_id) %>
        <%= genre.name %>
      </dd>
    <dt>税込み価格<br>(税抜き価格)</dt><dd><%= @item.price %>円</dd>
    <dt>販売ステータス</dt>
      <dd>
        <% if @item.is_active == true %>
          <p>販売中</p>
        <% else %>
          <p>販売停止中</p>
        <% end %>
      </dd>
  </dl>
</div>

<div>
  <%= link_to "編集する", edit_admin_item_path(@item) %>
</div>